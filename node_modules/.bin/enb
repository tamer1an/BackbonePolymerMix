#!/bin/sh
basedir=`dirname "$0"`

case `uname` in
    *CYGWIN*) basedir=`cygpath -w "$basedir"`;;
esac

if [ -x "$basedir/node" ]; then
  "$basedir/node"  "$basedir/../enb/bin/enb" "$@"
  ret=$?
else 
  node  "$basedir/../enb/bin/enb" "$@"
  ret=$?
fi
exit $ret
