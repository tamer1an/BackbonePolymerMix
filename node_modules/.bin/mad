#!/bin/sh
basedir=`dirname "$0"`

case `uname` in
    *CYGWIN*) basedir=`cygpath -w "$basedir"`;;
esac

if [ -x "$basedir/bash" ]; then
  "$basedir/bash"  "$basedir/../mad/bin/mad" "$@"
  ret=$?
else 
  bash  "$basedir/../mad/bin/mad" "$@"
  ret=$?
fi
exit $ret
