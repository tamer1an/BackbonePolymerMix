#!/bin/sh
basedir=`dirname "$0"`

case `uname` in
    *CYGWIN*) basedir=`cygpath -w "$basedir"`;;
esac

if [ -x "$basedir/node" ]; then
  "$basedir/node"  "$basedir/../borschik/bin/borschik" "$@"
  ret=$?
else 
  node  "$basedir/../borschik/bin/borschik" "$@"
  ret=$?
fi
exit $ret
